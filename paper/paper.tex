\PassOptionsToPackage{dvipsnames,svgnames,x11names}{xcolor}
% I want lots of colors!
\documentclass[runningheads]{llncs}
\usepackage[T1]{fontenc}
\usepackage{graphicx}%
\usepackage{multirow}%
\usepackage{amsmath,amssymb,amsfonts}%
%\usepackage{amsthm}%
\usepackage{mathrsfs}%
\usepackage[title]{appendix}%
\usepackage{xcolor}%
\usepackage{textcomp}%
\usepackage{manyfoot}%
\usepackage{booktabs}%
\usepackage{algorithm}%
\usepackage{algorithmicx}%
\usepackage{algpseudocode}%
\usepackage{listings}%
\usepackage[capitalize, nameinlink]{cleveref}
\usepackage{subfiles}
\usepackage{mathpartir}

% these two are probably only necessary for the draft
\usepackage{geometry}
\usepackage{pdflscape}
% these are needed for substantive stuff
\usepackage{stmaryrd}
\usepackage{ebproof}
\usepackage{mathtools}

\usepackage{draft} % local
\newnote{ly}{red}      % Li Yao
\newnote{lai}{cyan}    % Laura Israel
\newnote{nc}{violet}      % Nicholas Coltharp

% If you want to set draft to true or false, go to `directive.tex`
\input{directives.tex}

\usepackage{simplebnf}

%%%%%%%%%%%%%%%%%%%%%%%%%
% GUIDE TO LATEX MACROS %
%%%%%%%%%%%%%%%%%%%%%%%%%

% There are two languages:
%   + 𝐁, the base language
%   + 𝐋, the logic language
% - \L<X> is the symbol for language X

% - There are five "semantics symbols":
%   + 𝔼, for exact values and evaluation semantics
%   + 𝔸, for approximation values
%   + ℂ, for clairvoyance semantics
%   + 𝔻, for demand semantics
%   + 𝕃, for logic semantics
% - \S<X> is the semantics symbol X

% - \T<X> is the type X
% - \S<X> is the set of values for the type X
% - \t<X> is the term (or term-level construct) X
% - \s<X> is the semantic construct <X>

% - \T<X> is also the typing relation for language X
% - \E<X> is the evaluation relation for semantics X
% - \trans<X> is the translation from B to L corresponding to semantics X

% - Macros are generally n-ary; i.e., the macro \TThunk for thunk types takes
% one argument, the type argument.  If we need a zero-arity version of macro M,
% we use <M>Z.  For example, the zero-ary version of \TThunk is \TThunkZ.

%%%%%%%%%%%%%%%%%%%%%
% MACRO DEFINITIONS %
%%%%%%%%%%%%%%%%%%%%%

% draft stuff
\newcommand{\draftsection}

% paired delimiters
\DeclarePairedDelimiter{\bbrackets}{\llbracket}{\rrbracket}
\DeclarePairedDelimiter{\braces}{\lbrace}{\rbrace}
\DeclarePairedDelimiter{\ceil}{\lceil}{\rceil}
\DeclarePairedDelimiter{\floor}{\lfloor}{\rfloor}
\DeclarePairedDelimiter{\verts}{\lvert}{\rvert}

% theorem environments
% \newtheorem{lemma}{Lemma}
% \newtheorem{theorem}{Theorem}

% math notation
\newcommand{\N}{\mathbb{N}}
\newcommand{\set}[1]{\braces*{#1}}
\newcommand{\setbuilder}[2]{\set{{#1} \mid {#2}}}

% language symbols
\newcommand{\LB}{\mathbf{B}}
\newcommand{\LL}{\mathbf{L}}

% semantics symbols
\newcommand{\SA}{\mathbb{A}}
\newcommand{\SC}{\mathbb{C}}
\newcommand{\SD}{\mathbb{D}}
\newcommand{\SE}{\mathbb{E}}
\newcommand{\SL}{\mathbb{L}}

% semantic mappings on types
\newcommand{\Approx}[1]{\SA\bbrackets{#1}}
\newcommand{\Eval}[1]{\SE\bbrackets{#1}}

% types
\newcommand{\TBool}{\texttt{Bool}}
\newcommand{\TProd}[2]{\texttt{\(#1\)\ *\ \(#2\)}}
\newcommand{\TNat}{\texttt{Nat}}
\newcommand{\TList}[1]{\texttt{List \({#1}\)}}
\newcommand{\TThunkZ}{\texttt{Thunk}}
\newcommand{\TThunk}[1]{\texttt{\TThunkZ\ \(#1\)}}
\newcommand{\TM}[1]{\texttt{M\ \(#1\)}}

% terms
\newcommand{\tletZ}{\texttt{let}}
\newcommand{\tlet}[3]{\texttt{\tletZ\ \({#1}\)\ =\ \({#2}\)\ in\ \({#3}\)}}
\newcommand{\ttrue}{\texttt{true}}
\newcommand{\tfalse}{\texttt{false}}
\newcommand{\teq}[2]{\texttt{\(#1\)\ ==\ \(#2\)}}
\newcommand{\tle}[2]{\texttt{\(#1\)\ <=\ \(#2\)}}
\newcommand{\tif}[3]{\texttt{if\ \({#1}\)\ then\ \({#2}\)\ else\ \({#3}\)}}
\newcommand{\tpair}[2]{\texttt{(\(#1\),\ \(#2\))}}
\newcommand{\tadd}[2]{\texttt{\(#1\)\ +\ \(#2\)}}
\newcommand{\tnil}{\texttt{[]}}
\newcommand{\tcons}[2]{\texttt{\(#1\)\ ::\ \(#2\)}}
\newcommand{\tfoldrZ}{\texttt{foldr}}
\newcommand{\tfoldr}[5]{\tfoldrZ\texttt{(\(\lambda{#1}{#2}\).\(#3\),\ \(#4\),\ \(#5\))}}
\newcommand{\tthunkZ}{\texttt{thunk}}
\newcommand{\tthunk}[1]{\texttt{\tthunkZ\ \({#1}\)}}
\newcommand{\tundefined}{\texttt{undefined}}
\newcommand{\tlazyZ}{\texttt{lazy}}
\newcommand{\tlazy}[1]{\texttt{\tlazyZ\ \(#1\)}}
\newcommand{\tforceZ}{\texttt{force}}
\newcommand{\tforce}[1]{\texttt{\tforceZ\ \({#1}\)}}
\newcommand{\tcase}[4]{\texttt{case \(#1\) of thunk \(#2\) => \(#3\); undefined => \(#4\)}}
\newcommand{\ttickZ}{\texttt{tick}}
\newcommand{\ttick}[1]{\texttt{\ttickZ\ \({#1}\)}}
\newcommand{\tchoose}[2]{\texttt{\(#1\)\ ?\ \(#2\)}}
\newcommand{\tfreeZ}{\texttt{free}}
\newcommand{\tfree}[1]{\texttt{\tfreeZ\ \(#1\)}}
\newcommand{\tfail}{\texttt{fail}}
\newcommand{\tassert}[1]{\texttt{assert\ \(#1\)}}
\newcommand{\treturn}[1]{\texttt{return\ \(#1\)}}
\newcommand{\ttranspose}[1]{\texttt{transpose\ \(#1\)}}
\newcommand{\tfoldMZ}{\texttt{foldM}}
\newcommand{\tfoldM}[5]{\texttt{\tfoldMZ(\(\lambda {#1}{#2}.{#3}\),\ \(#4\),\ \(#5\))}}

% semantic sets
\newcommand{\SBool}{\set{\strue, \sfalse}}

% semantic values and operations
\newcommand{\strue}{\text{true}}
\newcommand{\sfalse}{\text{false}}
\newcommand{\sle}[2]{{#1} \leqslant {#2}}
\newcommand{\snle}[2]{{#1} \nleqslant {#2}}
\newcommand{\spair}[2]{({#1}, {#2})}
\newcommand{\snil}{\varepsilon}
\newcommand{\scons}[2]{(#1, #2)}
\newcommand{\sfoldr}[3]{\text{foldr}({#1}, {#2}, {#3})}
\newcommand{\sthunkZ}{\text{thunk}}
\newcommand{\sthunk}[1]{\text{\sthunkZ\ \({#1}\)}}
\newcommand{\sundefined}{\bot}

% relations
\newcommand{\approxof}[2]{{#1} \prec {#2}}

% typing rules

% base
\newcommand{\TB}[3]{{#1} \vdash^\LB {#2} : {#3}}
% logic
\newcommand{\TL}[3]{{#1} \vdash^\LL {#2} : {#3}}

% semantics

% evaluation
\newcommand{\EEU}[1]{\SE\bbrackets{#1}}
\newcommand{\EE}[2]{\EEU{#1}({#2})}
% clairvoyance
\newcommand{\EC}[4]{{{#1} \mathrel{;} {#2}} \Downarrow^\SC_{#3} {#4}}
% demand
\newcommand{\EDU}[1]{\SD\bbrackets{#1}}
\newcommand{\ED}[3]{\EDU{#1}({#2}, {#3})}
% demand foldr
\newcommand{\EDF}[5]{\EDU{#1}_{\text{foldr}}({#2}, {#3}, {#4}, {#5})}
% clairvoyance foldr
\newcommand{\ECF}[8]{{{#1} \mathrel{;} \lambda{#2}{#3}.{#4}, {#5}, {#6}} \Downarrow^{\text{\(\SC\)-\tfoldrZ}}_{#7} {#8}}
% logic
\newcommand{\EL}[3]{{{#1} \mathrel{;} {#2}} \Downarrow^\SL {#3}}
% logic foldr
\newcommand{\ELF}[7]{{{#1} \mathrel{;} \lambda{#2}{#3}.{#4}, {#5}, {#6}} \Downarrow^{\text{\(\SL\)-\tfoldrZ}} {#7}}
% logic foldM
\newcommand{\ELFM}[7]{{{#1} \mathrel{;} \lambda{#2}{#3}.{#4}, {#5}, {#6}} \Downarrow^{\text{\(\SL\)-\tfoldMZ}} {#7}}

% translations
\newcommand{\transC}[1]{\SC\floor{#1}}
\newcommand{\transD}[2]{\SD\floor{#1}({#2})}

% rules font
\newcommand{\rulename}[1]{\textsc{#1}}

\raggedbottom
%%\unnumbered% uncomment this for unnumbered level heads

\begin{document}

\title{When Hindsight and Foresight Are the Same:\\ Lazy Cost Analysis as
  Functional Logic Programming}

\author{Nicholas Coltharp\inst{1} \and
  Steven Libby\inst{2} \and Laura Israel\inst{3} \and
  Yao Li\inst{1}\thanks{Corresponding author.}}
\authorrunning{N. Coltharp et al.}

\institute{Portland State University, USA \\ \email{liyao@pdx.edu} \and
  University of Portland, USA \and University of Konstanz, Germany}

\maketitle

\begin{abstract}
  The abstract serves both as a general introduction to the topic and as a brief, non-technical summary of the main results and their implications. Authors are advised to check the author instructions for the journal they are submitting to for word limits and if structural elements like subheadings, citations, or equations are permitted.
\keywords{keyword1, Keyword2, Keyword3, Keyword4}
\end{abstract}

\section{Introduction}

In functional logic programming, one program contains two semantics: a
``forward'' nondeterministic semantics with \emph{choices}, and a ``backward''
demand-driven semantics with \emph{free variables}. Past research has shown the
usefulness of this feature in different contexts.\ly{TODO: Citations.} In this
paper, we show another useful yet distingushing example in lazy cost analysis.
In particular, we show that two different semantics proposed independently for
reasoning about the computation cost of lazy functional programs, namely the
clairvoyance semantics~\cite{clairvoyant,forking-paths} and the demand
semantics~\cite{bjerner1989,demand-semantics}, can both be represented by one
program in functional logic programming.

In lazy evaluation, a computation is driven by its demands. A computation is
stored as a \emph{thunk} that only gets evaluated when its value is demanded.
Even then, the computation is only minimally evaluated to the extent that's
sufficient for its demand. \ly{The pros and cons of lazy evaluation, briefly.}

% \subsection{what is lazy evaluation?}

\begin{itemize}
\item what is lazy evaluation?
\item why is it cool?
\item talk about cost analysis specifically
\item (maybe) briefly explain the standard semantics for laziness and its advantages/disadvantags
%\item briefly explain clairvoyance and demand semantics
%\item talk about our troubles with writing demand terms
\item explain the solution \ly{directly to solution}
  \item list of contributions \ly{Make sure to mention everything is formalized
        in Agda; all the examples are implemented in Curry (or PAKCS~\cite{pakcs}?).}
\end{itemize}

% The standard semantics for call-by-name uses a heap that binds variables to
% expressions; evaluating a variable amounts to evaluating the expression to
% which it is bound and then updating the heap to bind the variable to the
% resulting value. This is attractive because it reflects a straightforward
% implementation of call-by-name evaluation on an actual computer, but it has
% the drawback that it is non-compositional; that is, a derivation is not
% generally a simple composition of subterm derivations. Therefore, some have
% tried to develop semantics that are equivalent but also compositional.

% An early example is the \emph{demand semantics} of Bjerner and
% Holmstr{\"{o}}m. In this semantics, an open term is represented as a function
% that takes, in addition to a list of variable bindings, a \emph{demand} on the
% value to which the term will ultimately reduce; it produces a list of demands
% on its free variables. The demand represents, essentially, ``how much'' of the
% result will end up being used by the rest of the program. This semantics was
% originally presented without types; a version with types was published in
% STORYOFYOURLAZYFUNCTIONSLIFE.

% Another example of compositional semantics is the \emph{clairvoyance
% semantics} of Hackett and Hutton. The key idea of this semantics is that, when
% binding a variable to an expression, rather than deferring computation of the
% bound expression until it is needed, we \emph{immediately} choose
% nondeterministically to either evaluate the expression or discard the binding
% entirely. This semantics is equivalent to the standard semantics in the sense
% that, if \( t \Downarrow v \), then there is some derivation of this fact that
% is cost-equivalent to the derivation of the same in the standard semantics;
% and, furthermore, this derivation is the minimal such derivation.

% In STORYOFYOURLAZYFUNCTIONSLIFE, the fact that demand semantics is equivalent
% to the standard semantics was established by proving equivalence to
% clairvoyance semantics, which had already been proven equivalent to the
% standard semantics. However, this led to some difficulties with mechanized
% proofs. These proofs required writing demand functions by hand, a tedious and
% error-prone process. In fact, it required writing three versions of ``the same
% term'': a pure version, a clairvoyance version, and a demand version. Thus, we
% sought alternate means of expressing demand functions. In particular, we hoped
% to be able to write a term \emph{once} and automatically obtain the
% clairvoyance and demand versions of that term.

% Eventually, we hit on the idea of using \emph{functional logic programming}.
% Functional logic programming is a paradigm that combines the fundamental
% computational framework of functional programming with the nondeterminism and
% free variables found in logic programming.

% Our key idea is that, in a functional logic language, we can use
% nondeterminism to express directly the ``clairvoyance behavior'' of a term,
% and we can then obtain the corresponding ``demand behavior'' by representing
% the input demand as a free variable that is constrained to be an approximation
% of some actual input. Our main technical contribution is a proof that this
% approach does result in behavior that is equivalent to clairvoyance and demand
% semantics, respectively. This proof has also been formalized in Agda.

\section{Motivating example(s)}

\begin{itemize}
  \item a more in-depth discussion of lazy evaluation semantics, esp.
        clairvoyance and demand semantics
  \item give an example that models each semantics in some programming language
        (probably Curry)
  \begin{itemize}
    \item this will require displaying/explaining the basic infrastructure
    \item what example? should be fairly small. take-append?
  \end{itemize}
  \item discuss how this is very annoying to write in demand semantics
  \item show how, in a logic language, you can ``write both in one program''
  \item this will need a discussion of what logic programming is, and probably a
        brief explanation of the specific language we are using (probably Curry)
\end{itemize}

% In the absence of recursion, call-by-need is semantically equivalent to
% call-by-name. However, the two semantics have different performance
% characteristics: in particular, call-by-need is \emph{cost-optimal} in the
% sense that a call-by-need derivation of \( t \Downarrow v \) will never be
% larger than a call-by-name derivation of the same, and may be smaller. Thus, a
% natural way to study call-by-name evaluation \emph{in paticular} is to augment
% the evaluation relation with an additional parameter that tracks the cost of
% evaluation.

% worked example

\subfile{base-lang}

\subfile{logic-lang}

\subfile{equiv}

\section{Case studies}

\begin{itemize}
  \item various examples showing how our approach works
  \begin{itemize}
    \item exactly which examples will be based on experimentation: are there
          examples that Curry has no trouble with? examples that Curry does have
          trouble with?
    \item some candidates:
          \begin{itemize}
            \item take from head of a list (discussed earlier)
            \item insertion sort
            \item some amortized data structure
          \end{itemize}
  \end{itemize}
  \item (including at least one example using an amortized data structure)
  \item do they work? i.e., are they functionally correct?
  \item does Curry find all the solutions? does it ever get stuck?
  \item (maybe) how is the performance? are there any problems?
\end{itemize}

\section{Related works}

\section{Conclusion}

\bibliographystyle{splncs04}
\bibliography{yao,ref}

\newpage
\subfile{appendix}

\end{document}
